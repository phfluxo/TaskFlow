services:
  bot:
    build: .
    container_name: github_task_bot
    volumes:
      - .:/app
      - /app/.venv
      - ./data:/app/data
    env_file:
      - .env
    command: watchmedo auto-restart --directory=. --pattern="*.py" --recursive python main.py
    restart: unless-stopped